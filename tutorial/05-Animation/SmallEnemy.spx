var life int
onStart => {
	for i := 0; i < 30; i++ {
		wait 0.3
		clone
	}
}

onCloned => {
	life = 3
	setXYpos rand(-131, 131), 180
	show
	for {
		waitNextFrame
		changeYpos -48 * deltaTime
		if ypos < -180 {
			setXYpos rand(-131, 131), 180
		}
	}		
}
